# TiSLY Remote Test PoC — Phase 2 最小設定
# 使い方: cp .env.sample .env → 値を記入 → systemctl restart tisly-server
# 秘密情報は git にコミットしないこと

# 通信PoC 認証トークン（Web UI / RP2350 と同じ値）
# 生成: openssl rand -hex 16
REMOTE_TEST_TOKEN=

# Discord 通知（通知テストボタン用）
DISCORD_WEBHOOK_URL=
