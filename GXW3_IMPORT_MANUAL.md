# GX Works3 実機投入マニュアル

**TiSLY_HOME_Security_DEMO** を FX5U 実機へ投入するための手順書です。  
命令リストは `GXW3_PURE_COMMANDS.txt`（合格済み）を使用します。

---

## ■ GX Works3 新規プロジェクト作成手順

### 1. 新規プロジェクト

1. **GX Works3** を起動する。
2. メニュー **[プロジェクト]** → **[新規作成]** を選択する。
3. プロジェクト名に **TiSLY_HOME_Security_DEMO**（任意の名称でも可）を入力する。
4. 保存先フォルダを指定し **[OK]** をクリックする。

### 2. FX5U-24MR/ES を選択

1. **PLC シリーズ** で **FXCPU** を選択する。
2. **PLC タイプ** で **FX5U-24MR/ES** を選択する。
3. 他の設定はデフォルトのまま **[OK]** で確定する。

> **重要:** 本プログラムは **SM412 / SM413**（FX5U 専用クロックリレー）を使用します。  
> FX3U 等を選ぶと SM412 が認識されません。必ず **FX5U** 系を選択してください。

### 3. ラダー言語選択

1. プログラム言語の選択画面で **ラダー (LAD)** を選ぶ。
2. ラダー以外（ST / FBD 等）は選択しない。

### 4. MAIN プログラム作成

1. プロジェクトツリーで **プログラム** → **MAIN** が作成されていることを確認する。
2. **MAIN** をダブルクリックしてエディタを開く。
3. ツールバーで **書込モード**（鉛筆アイコン）になっていることを確認する。  
   モニタモードの場合は書込モードへ切り替える。

### 5. 命令リスト入力モードへ移動

1. MAIN エディタ上で右クリック → **[表示形式]** → **[命令入力]** を選択する。  
   （またはエディタ下部の表示形式切替から **命令入力** を選ぶ）
2. 1 行目（先頭行）をクリックしてカーソルを置く。
3. 既存の命令がある場合は **すべて選択して削除** し、空の状態にする。

---

## ■ GXW3_PURE_COMMANDS.txt の貼り付け方法

### 1. どこへ貼るか

| 項目 | 内容 |
|------|------|
| 貼り付け先 | **MAIN** プログラムの **命令入力ビュー先頭行** |
| 貼り付けファイル | リポジトリ直下の `GXW3_PURE_COMMANDS.txt` |
| 貼り付け範囲 | 先頭の `LD X0` から末尾の `END` まで **全文** |

**手順:**

1. `GXW3_PURE_COMMANDS.txt` をメモ帳等で開く。
2. **Ctrl + A** → **Ctrl + C** で全文をコピーする。
3. GX Works3 の MAIN 命令入力ビュー先頭行で **Ctrl + V** で貼り付ける。

貼り付け後、以下の命令が含まれていることを目視確認する。

```
LD    X0  …（先頭）
…
ORB
OUT   M20
…
OUT   Y0  …（Y0 はこの 1 回のみ）
…
END   …（末尾）
```

### 2. 変換ボタン

1. 貼り付け完了後、命令入力ビュー内の任意の行を選択する。
2. ツールバーの **[命令入力→ラダー変換]** ボタンをクリックする。  
   （ショートカット: **F4** キー）
3. 変換が完了すると、ラダー図ビューにネットワークが展開される。
4. エラーが表示された場合は後述の **トラブルシュート** を参照する。

### 3. ラダー化確認方法

変換後、以下を確認する。

| 確認項目 | 期待結果 |
|----------|----------|
| ネットワーク数 | 複数段（Network）に分割されている |
| SM412 / SM413 | エラーなく表示される |
| OUT Y0 | **1 か所のみ**（M20 経由） |
| END | 最終ネットワークに存在する |
| コンパイル | **[変換]** → **[プログラムチェック]** でエラー 0 件 |

**プログラムチェック手順:**

1. メニュー **[変換]** → **[プログラムチェック]** を実行する。
2. 結果ウィンドウに **エラー 0、警告 0**（または Y0 二重コイル警告なし）であることを確認する。
3. 問題なければ **[オンライン]** → **[書込]** で PLC へ転送する。

---

## ■ 動作確認手順

シミュレータまたは実機 RUN 状態で、入力 X を順に操作し、出力 Y と内部リレー M を確認する。

### STEP1 — X0 ON

| 操作 | X0（セレクタスイッチ）を ON |
|------|------------------------------|

**期待結果:**

| デバイス | 状態 |
|----------|------|
| M0 | **ON**（警戒中 SET） |
| Y0 | **低速点滅**（SM413 = 1 秒周期） |

---

### STEP2 — X2 ON

| 操作 | X2（ビームセンサー 1 / 外周）を ON ※X0 は ON のまま |
|------|------------------------------------------------------|

**期待結果:**

| デバイス | 状態 |
|----------|------|
| M1 | **ON**（外周警報保持） |
| Y1 | **点灯** |
| Y2 | **点滅**（SM413 = 1 秒周期） |

---

### STEP3 — X3 ON

| 操作 | X3（ビームセンサー 2 / 近接）を ON ※X0 は ON のまま |
|------|------------------------------------------------------|

**期待結果:**

| デバイス | 状態 |
|----------|------|
| M2 | **ON**（近接警報保持） |
| Y3 | **点灯** |
| Y4 | **点灯** |
| Y0 | **高速点滅**（SM412 = 0.1 秒周期） |

> Y0 は M2 優先のため、STEP1 の低速点滅から高速点滅へ切り替わる。

---

### STEP4 — X1 ON

| 操作 | X1（非常停止ボタン）を ON |
|------|---------------------------|

**期待結果:**

| デバイス | 状態 |
|----------|------|
| M0 | **OFF** |
| M1 | **OFF** |
| M2 | **OFF** |
| Y0～Y4 | **全消灯** |

---

### STEP5 — X0 OFF

| 操作 | X0 を OFF（X1 は OFF に戻してからでも可） |
|------|-------------------------------------------|

**期待結果:**

| デバイス | 状態 |
|----------|------|
| M0 / M1 / M2 | **OFF**（保持解除） |
| Y0～Y4 | **全消灯** |

---

## ■ トラブルシュート

### ケース1 — 貼り付けできない

**症状:** Ctrl + V が効かない、または貼り付けても命令が反映されない。

**原因と対処:**

| 原因 | 対処 |
|------|------|
| モニタモードになっている | ツールバーで **書込モード** に切り替える |
| ラダー図ビューのまま | **命令入力** 表示形式へ切り替えてから貼り付ける |
| 既存命令と混在 | MAIN 内の命令を全選択 → 削除 → 再度全文貼り付け |
| ファイル形式 | `GXW3_PURE_COMMANDS.txt` を使用（`GXW3_INPUT_FINAL.txt` ではない） |

---

### ケース2 — ORB エラー

**症状:** 変換時またはプログラムチェック時に **ORB** 付近で構文エラー。

**原因:** ORB の直前に並列ブロック（LD ～ LD）が 2 つ必要。命令の欠落や行順序の乱れ。

**対処:**

1. `GXW3_PURE_COMMANDS.txt` を **改変せず** 再度全文貼り付けする。
2. ORB 直前の命令列を確認する。

```
LD    M2
AND   SM412
LD    M0
ANI   M2
AND   SM413
ORB
OUT   M20
```

3. 上記 6 行が連続していることを確認する。
4. 手動修正する場合は、2 つの LD ブロックを 1 つの OR 回路としてラダー上で再構成する。

---

### ケース3 — SM412 認識しない

**症状:** SM412 / SM413 が未定義デバイス、またはコンパイルエラー。

**原因と対処:**

| 原因 | 対処 |
|------|------|
| PLC タイプが FX5U 以外 | プロジェクト設定を **FX5U-24MR/ES** に変更する |
| 旧 FX 系列の特殊リレー名を使用 | **M8012 / M8013 は使用しない**。SM412 / SM413 が正しい |
| タイプミス | `SM412`（0.1 秒クロック）、`SM413`（1 秒クロック）を確認 |

> FX5U における対応: SM412 ≒ 旧 M8012（0.1 秒）、SM413 ≒ 旧 M8013（1 秒）

---

### ケース4 — Y0 二重コイル警告

**症状:** プログラムチェックで **Y0 の二重コイル** 警告またはエラー。

**原因:** OUT Y0 が 2 か所以上存在する。

**対処:**

1. プログラム内で `OUT Y0` を検索する。
2. **1 か所のみ**（`LD M20` → `OUT Y0`）であることを確認する。
3. Y0 への直接 SET/RST/OUT が他にないか確認する。  
   非常停止時の `RST Y0`（X1 処理内）は出力リセット命令であり、二重コイルとは別扱い。
4. `GXW3_PURE_COMMANDS.txt` を使い直す。Y0 制御は内部リレー **M20** 経由の単一コイル構成。

---

### ケース5 — END 不足エラー

**症状:** **END 命令がない**、または **プログラムの終端が不正** というエラー。

**原因:** 貼り付け時に末尾の `END` が欠落した。

**対処:**

1. 命令入力ビューの **最終行** に `END` があることを確認する。
2. ない場合は最終行に `END` を追加する。
3. 再度 **[命令入力→ラダー変換]** → **[プログラムチェック]** を実行する。

---

## 投入完了

以上の手順と動作確認（STEP1～STEP5）がすべて問題なければ、実機またはシミュレータへの書込み・RUN が可能です。

---

**GX Works3で実機シミュレーション開始可能**
