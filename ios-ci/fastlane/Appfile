# TiSLY iOS — Fastlane Appfile
# ASC API キー運用時は apple_id 不要。team_id は任意（APPLE_TEAM_ID）。

app_identifier(ENV["IOS_BUNDLE_ID"] || "jp.tisly.app")
team_id(ENV["APPLE_TEAM_ID"]) if ENV["APPLE_TEAM_ID"].to_s.strip != ""
