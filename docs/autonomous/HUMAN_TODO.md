# 智紀さんの ToDo（短く）

## 本番前に必須

1. **Gmail アプリパスワード** を `.env` に設定 → App Hub でテスト送信
2. **VAPID 鍵** を生成（`cd server && npm run vapid:setup`）
3. **JWT_SECRET** を本番用に再生成
4. **CUSTOMER_DEMO_PASSWORD** を本番では変更 or 無効化

## できれば早め

5. TOMS 標準フォーマット仕様書の共有 → AI 見積 API 接続設計
6. Google Calendar OAuth（Business カレンダー連携を使う場合）
7. VPS デプロイ確認（`scripts/deploy.sh`）

## スマホで確認してほしい（実務 PWA）

### ログイン

- 会社コード `TOMS001`、surveyor ユーザーで `/app` にログイン

### 確認の順番

1. `/app` — 「今日使うアプリ」に現調・見積の大きいカードが見えるか
2. 下部ナビで **現調** → **見積** → **アプリ一覧** と移動できるか
3. `/survey-v1` — 新規案件 → 写真/メモ/部材 → 「見積へ送る」
4. `/estimate-v1` — 見積もり作成待ち → 見積作成 → 数量/単価 → 確定 → PDF プレビュー
5. 見積詳細の「現調の内容を見る」で元の現調に戻れるか
6. 上部の **戻る** / **進む** ボタンが使えるか

### iPhone ホーム画面に追加（Safari）

各 URL を Safari で開き、共有 → **ホーム画面に追加** で専用アプリ化できます。

| 追加する URL | 期待する見え方 |
|--------------|----------------|
| `/app` | TiSLY 業務アプリ一覧（下部ナビ付き） |
| `/survey-v1` | 現調専用アプリ（緑ヘッダー・下部ナビ） |
| `/estimate-v1` | 見積専用アプリ（青ヘッダー・下部ナビ） |

**確認項目**

- [ ] ステータスバー下の余白が自然か（ノッチ・ホームインジケータ）
- [ ] 下部ナビが指で押しやすいか
- [ ] スタンドアロン表示で戻る/進む/アプリ一覧が使えるか
- [ ] ホーム画面から起動してもログイン状態が維持されるか

## 確認してほしい画面

- `/app` — 実務アプリのカードが見やすいか
- `/survey-v1` — 現場で片手操作しやすいか
- `/estimate-v1` — 見積の流れがわかりやすいか

## やらなくていい（Cursor が進める）

- 自走管理ドキュメントの更新
- 仮データでの UI 改善
- 単体テストの実行
